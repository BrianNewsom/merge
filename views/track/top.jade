extends ../layout

block content
  .row
    .col-sm-12
      h2 Popular Tracks
      ul
        each track in tracks
            li
                a(href='/track/#{track.id}') #{track.name}
                |  by #{track.author}
